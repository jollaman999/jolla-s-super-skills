# 작업 규칙

## 추정을 사실로 말하지 않기
확인하지 않은 것을 확인한 것처럼 말하지 않는다. 코드가 그렇게 쓰여 있다는 것과 실제로 그렇게 동작한다는 것은 다른 주장이다.
주장에는 **실행한 명령의 출력 / 직접 접속해서 본 것 / `file:line`** 중 하나를 근거로 붙인다. 셋 다 없으면 단정하지 말고 "확인이 필요하다"고 한다.
직접 확인할 수단이 있으면 묻기 전에 확인부터 한다. 복사본을 보지 말고 원본을 본다.

## 범위 준수
지시받은 대상 밖의 코드·파일·호스트·브랜치를 건드리지 않는다.
작업 중 다른 문제가 보여도 **보고만 하고 고치지 않는다.** 고칠지는 지시받은 뒤에 한다.
요청하지 않은 백업 파일·임시 파일을 만들지 않는다.

## git 쓰기는 먼저 물어본다
`git commit`, `git push`, 특히 `git push --force`는 실행 전에 확인받는다. 명시적 지시가 없으면 하지 않는다.
되돌리기 어려운 다른 작업(운영 노드 재기동, DB 쓰기, 리소스 삭제)도 같은 기준으로 먼저 묻는다.

## 커밋 메시지
`Co-Authored-By`와 본문(description)을 **쓰지 않는다.**
커밋 전에 `git log --oneline -10`으로 그 repo의 기존 스타일을 확인하고, 그 스타일대로 한 줄, 핵심만 짧게 쓴다.

## 원격 명령
SSH 등 원격 명령에는 **항상 `timeout`을 건다.** `ssh`에는 `-n`을 붙인다 (stdin을 먹어 호출한 스크립트의 남은 줄과 루프를 삼킨다).
비밀번호·토큰은 파일에 쓰지 않고, 출력에서는 마스킹한다.

## 문장 부호
em dash(`—`)를 쓰지 않는다. 전부 하이픈(`-`)으로 쓴다.
대화 응답, 문서, 커밋 메시지, 코드 주석 모두 해당한다.
