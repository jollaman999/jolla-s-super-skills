# 실증 검증 보고

대상: <repo> · 방식: 실노드 접속 / 로컬 구동 · 일시: <YYYY-MM-DD>

## 요약
| verdict | 수 |
|---------|-----|
| pass (실증) | |
| pass (정적) | |
| disputed (반증됨) | |
| fail | |
| unknown (검증 불가) | |

## 호스트별 결과 - 사이트 간 차이가 보이게
| id | 대상 | 방법 | 사이트A<br>110.220 | 사이트B<br>228.234 | 내부노드 |
|----|------|------|------|------|------|
| VF-01 | 에이전트 기동 | ssh | pass | pass | **fail** |
| VF-02 | 메트릭 적재 | db | pass | pass | unknown |

> 한 호스트만 다르면 그 자체가 결론이다. 원인까지 파고든 내용을 아래에 적는다.

## 증거
### VF-01 @ 내부노드 (fail)
```
$ ssh ... 'docker inspect app-svc --format {{.State.Health.Status}}'
unhealthy
$ ssh ... 'docker logs --tail 20 app-svc'
<로그 발췌 - 시크릿 마스킹됨>
```

## disputed 상세
| id | 반증 사유 | 다시 확인할 것 |
|----|-----------|----------------|

## 검증하지 못한 것과 이유
| 대상 | 왜 못 했나 | 필요한 것 |
|------|------------|-----------|
| VF-06 | 로컬 구동이라 GPU 없음 | 실노드 접속 |

> 이 절은 비워두지 않는다. 생략하면 "전부 검증됨"으로 오독된다.

## 범위 밖이라 손대지 않은 것
| 발견 | 위치 | 왜 안 건드렸나 |
|------|------|----------------|

## 다음에 볼 것
-
