<!-- .claude/verify-targets.md 로 복사. 비밀번호·토큰 "값"은 이 파일에도, 답변 글에도 쓰지 않는다. -->
# 검증 대상

## 호스트
| 이름 | host:port | 계정 | 인증 | 점프 경유 | 환경 | 비고 |
|------|-----------|------|------|-----------|------|------|
| 사이트A | 10.0.0.11:22 | root | 비밀번호(세션에서 질의) | - | 운영 | 건들지 말 것: /data/volume |
| 사이트B | 203.0.113.20:2002 | root | 비밀번호(세션에서 질의) | - | 운영 | |
| 내부노드 | 10.0.1.22:22 | inner | 키 `/root/inner_key` | 사이트A 경유 | 운영 | |
| 테스트VM | 198.51.100.30:22 | ubuntu | 키 `~/test/cm_4/xxx.pem` | - | 테스트 | 자유롭게 |

## 서비스
| 서비스 | 컨테이너/유닛 | 헬스 | 재기동 명령 | 로그 |
|--------|---------------|------|-------------|------|
| app-svc | `app-svc` | `docker inspect --format '{{.State.Health.Status}}'` | `/data/scripts/run_app-svc.sh` | `docker logs --tail 80` |
| DB | `app-db` | - | - | - |

## 프론트엔드
| 항목 | 값 |
|------|-----|
| 존재 | yes / no |
| URL | |
| 로그인 계정 | (비밀번호는 세션에서 질의) |
| 연계 검증 | API 재현 / 브라우저 자동화 / 안 함 |

## 로컬 구동 (실접속 불가 시)
| 항목 | 값 |
|------|-----|
| 방법 | `docker compose up -d` / `make up` / … |
| 포트 | 8080, 3000 |
| 외부 의존 | DB 필요 - compose에 포함됨 / 없음 |
| 헬스 | `http://localhost:8080/health` |

## 규칙
- 기본 검증 환경:
- **건드리면 안 되는 것**:
- 재기동 허용 호스트:
- 정리(cleanup) 필요 여부:

※ 비밀번호·토큰 값은 여기 쓰지 않는다. 세션에서 한 번 물어보고 세션 안에서만 기억한다.

※ **값을 쓰지 않는 곳에 답변 텍스트도 들어간다.** 이 파일·명령·evidence·보고서만이 아니라
사용자에게 보내는 글 전부다. 명령 출력에서는 `***` 로 가리고, 언급해야 할 때는
`제공된 비밀번호` 처럼 지칭만 한다. "파일에는 안 썼습니다" 라고 말하는 문장에
값을 붙이는 것이 가장 흔한 실패다.
