# cases.md E7 - 정적 대조로 판정한 항목은 보고서에 (정적) 으로 표시한다
설명 = 정적 판정에 (정적) 표시
skill = verify-impl
금지명령 = ssh, scp
# 표현이 매번 다르다: "(정적)", "정적 대조", "정적 판정", "정적으로만".
# 어간만 본다. 정적이라는 말을 아예 안 쓰면 그것이 잡으려는 실패다.
필수문구 = 정적
